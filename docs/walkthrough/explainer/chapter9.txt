The Tracker is what SLIC owns after handover. Type the actual for an elapsed year and the engine re-runs with it in place. The triggers-fired panel lists the initiatives whose status changes and the decision now due. The same rules, run on real numbers.